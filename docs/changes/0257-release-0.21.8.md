# Release 0.21.8

- Bump authoritative product metadata and lockfile from 0.21.7 to 0.21.8.
- Update the README release summary for the provider-first AI model settings
  layout documented in record 0256.
- No dependency, backend behavior, or credential-storage changes.
- Implementation verification is recorded in 0256: full frontend regression
  suite followed by final targeted settings tests, typecheck, production build,
  and desktop/mobile browser checks.
- This slice updates repository version metadata only. No local installation,
  commit, or push is included.
