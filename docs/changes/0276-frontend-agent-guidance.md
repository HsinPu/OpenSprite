# Frontend agent guidance consolidation

- Merge the supplied 85-point frontend standard into 12 topic sections, replacing the overlapping older frontend rules.
- Preserve repository direction, ownership, data safety, change workflow, verification commands, and generated-file boundaries.
- Add component-library boundaries, state/component responsibility, async states, destructive confirmations, overflow/date consistency, and responsive/accessibility requirements.
- Theme examples remain conditional; established intentional brand tokens take precedence.
- Documentation-only change; verify preserved repository prefix and Markdown whitespace. No application, dependency, version, or deployment changes.
