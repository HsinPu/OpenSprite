# Skills popup container

- Render workspace, inherited-policy, import, and scan-tooltip popups inside the settings overlay container.
- Follow the same portal boundary used by existing model and MCP settings, avoiding popups behind the settings dialog.
- Keep body fallback for standalone rendering.
