# 自訂 Provider 設定介面

## 變更

- 設定頁支援新增／編輯／移除自訂供應商，保留內建連線操作。
- 模型探索、手動模型、穩定 key 編輯與移除接上後端 revision 與引用保護。
- Provider／模型分頁回應經 strict 驗證後完整替換畫面；失敗保留既有資料。
- 三語安全錯誤與復原提示；表單 label 關聯、390px Drawer、桌面 Modal。
- 認證方式使用 Ant 單選按鈕，避免兩個選項仍依賴浮層定位。

## 驗證

前端完整 423 項測試通過；typecheck 通過。隔離內嵌瀏覽器驗證名稱／URL 輸入、
無認證切換、失敗保留輸入及 390px 模型管理無水平溢出。
瀏覽器使用模擬 API，不宣稱已測試真實遠端供應商 CRUD。
未 push、未更新本機安裝。
