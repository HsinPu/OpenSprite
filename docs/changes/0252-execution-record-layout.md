# Execution record narrow-panel layout

- Place each event timestamp below its label in a two-column icon/content grid, independent of viewport size so manually narrowed panels remain readable.
- Use a zero-minimum content track and allow long labels and errors to wrap instead of causing horizontal overflow.
- No run behavior, error handling, version or installed runtime changes.
