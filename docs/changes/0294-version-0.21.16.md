# Version 0.21.16

Update the authoritative backend package version and matching uv lock entry
from 0.21.15 to 0.21.16 for the provider tool policy and native non-streaming
tool support delivered in commit 1018f5c2.

No dependency versions, user settings, credentials or runtime behavior change.
This version-metadata update does not deploy the installed application, create
a Git commit, push changes or publish a release.

Verification: matching package/lock versions, offline lock validation and
`git diff --check`.
