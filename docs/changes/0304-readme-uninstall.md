# README 補上解除安裝指令

產品版本由 `0.21.19` 提升至 `0.21.20`；同步更新後端套件、uv 鎖檔與 README。

## 調整內容

- 在主 README 的安裝說明後加入 Windows 與 Linux 解除安裝指令，直接使用安裝目錄內附的腳本，不需要重新 clone 原始碼。
- Windows 指令透過 PowerShell 的程序執行原則啟動腳本，保留既有確認提示；Linux 說明使用原安裝帳號、不要使用 sudo，並沿用安裝時的 XDG 目錄設定。
- 說明預設保留 `.opensprite` 的對話、設定與憑證，另外列出永久移除資料的明確選項及確認方式。
- 說明解除安裝不移除 Git、Node.js 與 uv，並連結各平台的詳細文件。

## 驗證

- 對照兩個平台的安裝與解除安裝腳本，確認安裝包包含腳本、README 路徑與參數正確，且資料保留與確認流程符合目前實作。
- README 中 PowerShell 指令的語法解析通過。
- 後端套件、uv 鎖檔與 README 的版本一致；離線鎖檔檢查及 `git diff --check` 通過。

本次只修改文件及版本資訊，未執行真實解除安裝或刪除使用者資料。
