# Release 0.19.2

Patch release containing the draft preservation and locale/notification lifecycle
fixes in records 0215 and 0216. Update authoritative package metadata, lock
metadata, README, Skills contract version and version assertions.

Verification: 35 version/app contract tests passed; offline lock, dependency
compatibility and git diff --check passed.
No dependency changes, installation, commit or push.
